# frozen_string_literal: true

module FloopFloop
  # Gem semver, kept in sync with the latest `v*` git tag and the
  # `Gem::Specification#version` in floopfloop.gemspec.
  VERSION = "0.1.0.alpha.2"
end
